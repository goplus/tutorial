### 포인터의 제로 값 이해하기
정의되었지만 값이 할당되지 않은 포인터의 제로 값은 nil입니다.

포인터 second는 정의되었지만 값이 초기화되지 않았으며, println 함수를 사용하여 출력됩니다. 그런 다음 주소 연산자를 사용하여 first 변수에 대한 포인터를 생성하고, second의 값을 다시 출력합니다.

값이 할당되지 않은 포인터를 따라가면 런타임 오류가 발생합니다.