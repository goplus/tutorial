创建错误最简单的方式是调用 errors.New，它为给定的错误消息返回一个新的错误。整个 errors 包只有四行代码：
---
errorString 的底层类型是结构体而不是字符串，这是为了保护其表示不被意外（或故意）修改。而指针类型 *errorString（而非 errorString 本身）满足 error 接口的原因是，这样每次调用 New 都会分配一个不同的错误实例，与其他任何错误都不相等。
