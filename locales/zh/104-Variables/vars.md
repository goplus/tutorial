在 XGo 中，_变量_ 需要显式声明，编译器会利用它们来检查函数调用的类型正确性等。
---
`var` 用于声明一个或多个变量。
---
你可以一次声明多个变量。
---
XGo 会自动推断已初始化变量的类型。
---
声明时未进行初始化的变量会被赋予_零值_。例如，`int` 的零值是 `0`。
---
`:=` 语法是声明并初始化变量的简写形式，例如在这个例子中等价于 `var f string = "apple"`。
