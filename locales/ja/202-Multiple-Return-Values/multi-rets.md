XGo は_複数の戻り値_を組み込みでサポートしています。この機能は慣用的な XGo コードで頻繁に使用されます。例えば、関数から結果とエラー値を同時に返す場合などです。
---
この関数シグネチャの `(int, int)` は、関数が 2 つの `int` を返すことを示しています。
---
ここでは、呼び出しから返された 2 つの異なる値を_多重代入_で使用しています。
---
戻り値の一部だけが必要な場合は、ブランク識別子 `_` を使用します。
