次に、アサートされた型 T がインターフェース型の場合、型アサーションは x の動的型が T を満たすかどうかを検査します。検査が成功しても、動的値は取り出されません。結果は同じ型と値のコンポーネントを持つインターフェース値のままですが、結果はインターフェース型 T を持ちます。つまり、インターフェース型への型アサーションは式の型を変更し、異なる（通常はより大きい）メソッドセットにアクセスできるようにしますが、インターフェース値内部の動的型と値のコンポーネントは保持されます。
