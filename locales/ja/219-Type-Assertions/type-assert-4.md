多くの場合、インターフェース値の動的型が不明であり、特定の型かどうかをテストしたいことがあります。型アサーションが 2 つの結果を期待する代入に現れる場合、例えば以下の宣言のように、操作は失敗時に panic せず、代わりに成功を示すブール値の追加の第 2 結果を返します：
---
第 2 の結果は慣例的に ok という名前の変数に代入されます。操作が失敗した場合、ok は false になり、第 1 の結果はアサートされた型のゼロ値に等しくなります。この例では nil の *bytes.Buffer です。
