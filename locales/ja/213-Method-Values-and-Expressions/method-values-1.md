### メソッド値
---
メソッド値を使うと、メソッドを特定のオブジェクトに束縛し、オブジェクトを暗黙的に含んだ状態で通常の関数のように呼び出せます。クロージャに似ています。例えば：
---
式 `hello.Say` はメソッド値を作成し、`Say` メソッドを特定の変数 `hello` に束縛して、型を `func()` にします。したがって、メソッド値は関数のパラメータとして渡すことができます。
