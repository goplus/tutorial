_Switch 文_ は多くの分岐にまたがる条件式を表現するために使用されます。
---
これは基本的な `switch` の例です。
---
同じ `case` 文でカンマ区切りで複数の式を使用できます。この例ではオプションの `default` ケースも使用しています。
---
式なしの `switch` は if/else ロジックを表現する別の方法です。ここでは `case` 式が定数でなくてもよいことも示しています。
---
XGo の switch はデフォルトで各 case の末尾で自動的に break します。fallthrough を使うと、後続の case のコードを強制的に実行できます。
---
`fallthrough` 付きの `switch`：
